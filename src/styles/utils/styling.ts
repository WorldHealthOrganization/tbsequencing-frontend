const HEADER_HEIGHT = 64;

export const FULL_HEIGHT_WITHOUT_HEADER = `calc(100% - ${HEADER_HEIGHT}px)`;

export const FLEX_CENTERING = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
};
