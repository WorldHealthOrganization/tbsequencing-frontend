import { css } from '@emotion/react';

export const section = css({
  display: 'flex',
  justifyContent: 'space-between',
});

export const label = css({
  fontSize: 16,
});

export const value = css({
  fontWeight: 700,
  textAlign: 'right',
});

export const percentsValue = css({
  width: 50,
});

export const noPercentsMargin = css({
  marginRight: 77,
});

export const separator = css({
  fontWeight: 700,
  marginLeft: 8,
  marginRight: 16,
});

export const legendWrapper = css({
  display: 'flex',
  marginBottom: 8,
  justifyContent: 'center',
});
