import { css } from '@emotion/react';
import appColors from '../../../../../../styles/colors';

export const nameWithVerdictContainer = css({
  display: 'flex',
  alignItems: 'center',
});

export const editIconSx = {
  fill: appColors.secondary.whoBlue,
};
