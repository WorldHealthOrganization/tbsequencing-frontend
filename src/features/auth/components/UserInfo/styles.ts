import { css } from '@emotion/react';

export const container = css({
  display: 'flex',
});

export const userVerificationContainer = css({
  flex: 1,
});

export const accountDetailsContainer = css({
  marginLeft: 16,
  flex: 1,
});
