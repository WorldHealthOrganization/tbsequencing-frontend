import { css } from '@emotion/react';

export const header = css({
  marginTop: 96,
  textAlign: 'center',
});

export const container = css({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
});

export const headerWrapper = css({
  maxWidth: '35%',
  display: 'flex',
  flexDirection: 'column',
  flexWrap: 'wrap',
  justifyContent: 'center',
  alignItems: 'center',
});

export const okayBtn = css({
  width: '70px',
  marginTop: 30,
  marginBottom: 95,
});
