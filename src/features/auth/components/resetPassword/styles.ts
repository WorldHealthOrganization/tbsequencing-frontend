import { css } from '@emotion/react';

export const header = css({
  marginTop: 40,
  marginBottom: 40,
  // TODO refactor and move to PageHeader component
});

export const infoMsg = css({
  marginBottom: 32,
});

export const signUpBtn = css({
  marginLeft: 16,
});

export const paper = css({
  width: '60%',
});

export const container = css({
  paddingLeft: 54,
  paddingRight: 54,
});

export const btnContainer = css({
  marginTop: 32,
});
