import { css } from '@emotion/react';

export const container = css({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
});

export const header = css({
  textAlign: 'center',
  marginTop: 156,
  marginBottom: 176,
  maxWidth: 798,
});
