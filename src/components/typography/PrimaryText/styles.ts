import { css } from '@emotion/react';
import { font, fontSizes } from '../fontSizes';

export const primaryTextStyles = css({
  fontFamily: font.regular,
  fontSize: fontSizes.primary,
  lineHeight: '20px',
});
