import { css } from '@emotion/react';
import { fontSizes, font } from '../fontSizes';

export const h1Styles = css({
  fontFamily: font.bold,
  fontSize: fontSizes.h1,
  lineHeight: `${40}px`,
  fontWeight: 'bold',
});
