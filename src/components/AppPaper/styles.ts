import { css } from '@emotion/react';
import appColors from '../../styles/colors';

export const container = css({
  padding: 24,
  boxShadow: '0px 0px 4px rgba(201, 221, 234, 0.5)',
  backgroundColor: appColors.neutral.white,
});
