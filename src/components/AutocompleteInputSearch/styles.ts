import appColors from '../../styles/colors';

const tableStyles = {
  filterIcon: ({
    cursor: 'pointer',
    fontSize: 20,
    marginTop: '2px',
    '&.MuiSvgIcon-root': {
      color: appColors.secondary.whoBlue,
    },
  }),
};

export {
  tableStyles,
};
