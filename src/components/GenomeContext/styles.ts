import appColors from '../../styles/colors';

export const genomeContext = {
  backgroundColor: appColors.tints.whoBlue.lightTransparent,
  marginRight: '0px',
  marginLeft: '0px',
  borderLeft: `2px solid ${appColors.secondary.whoBlue}`,
  borderRight: `2px solid ${appColors.secondary.whoBlue}`,
};

export const selectedBar = `fill: ${appColors.secondary.whoYellow}`;
export const defaultBar = `fill: ${appColors.tints.whoPurple.lighter}`;
